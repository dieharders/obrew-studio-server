# Use Python version 3.12.8
#
# Pin setuptools to avoid pkg_resources deprecation warning (pyinstaller ?).
# Once the dep is identified and updated/removed this can be removed too.
setuptools<81
# Document reading
PyMuPDF==1.25.5
# Handling ai responses
json_repair==0.40.0
# UUID
nanoid==2.0.0
# QR-code image gen
PyQRCode==1.2.1
pypng
# .env vars
python-dotenv==1.0.0
# For type safety
pydantic==2.11.3
# For downloading models
huggingface-hub==0.22.2
# For servers
fastapi==0.115.0
uvicorn==0.29.0
httpx==0.27.0
sse-starlette==1.6.5
starlette-context==0.3.6
# HTML templating
pywebview==5.3.2
# For vector storage (direct ChromaDB usage)
chromadb==1.3.5
chroma-hnswlib==0.7.3  # Required binary for ChromaDB vector similarity (their own fork)
# Pin numpy to 1.x for PyInstaller compatibility (numpy 2.x has _core structure issues), used by ChromaDB
numpy<2
# Required by chromadb's dependencies (pandas, etc.)
python-dateutil>=2.8.0
distro>=1.8.0
# Optional file format support (lazy imports)
python-docx==1.1.0  # .docx files
python-pptx==0.6.23  # .pptx files
striprtf==0.0.26  # .rtf files
# Sentence boundary detection (no runtime downloads required, unlike NLTK)
pysbd==0.3.4
# LlamaParse cloud service (optional, requires API key)
# llama-parse==0.4.9
# For building single exe python
pyinstaller==6.11.1
python-multipart==0.0.18
# GPU Profiling
WMI==1.5.1; sys_platform == 'win32'  # Windows only
GPUtil==1.4.0
# Image processing (vision preprocessing for llama.cpp compatibility)
Pillow>=10.0.0
# macOS Platform Support
pyobjc-framework-Cocoa==10.3.1; sys_platform == 'darwin'  # macOS only - for AppKit screen resolution
